# App environment
NODE_ENV=development
PORT=3000
SESSION_SECRET=changeme

# MySQL database config (local dev)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=123456
DB_NAME=aplinova_dev

# Mail settings
MAIL_SANDBOX=true
MAILHOST=smtp.example.com
MAILPORT=587
MAILUSER=your_smtp_username
MAILPASS=your_smtp_password

# Timezone (optional override)
TZ=UTC